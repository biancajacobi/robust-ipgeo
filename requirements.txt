requests>=2.31
pandas>=2.0
numpy>=1.26
scipy>=1.11
matplotlib>=3.8

# Readers for the local LITE databases (offline, independent lines):
geoip2>=4.8        # reads MaxMind/DB-IP .mmdb (pulls in maxminddb)
IP2Location>=8.10  # reads IP2Location .BIN

# Tests
pytest>=8.0

# Storage is deliberately text-based (CSV + JSONL, stdlib only): no binary DB
# needed, which keeps the forensic provenance core (data/store.py) light on
# dependencies.
